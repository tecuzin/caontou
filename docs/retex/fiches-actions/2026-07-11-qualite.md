# Fiche action — Qualité (11/07/2026)

17 actions issues de l'[audit qualité](../2026-07-11-analyse-qualite.md). Chaque ligne
relie une action (ticket Epiq, swimlane Todo) à son **gain attendu**, puis au **gain
obtenu** une fois traitée. Statut : ⬜ à faire · 🟦 en cours · ✅ fait.

## Priorité 1 (structurant)

| # | Action | Domaine | Gain attendu | Gain obtenu | Statut |
|---|---|---|---|---|---|
| 1 | TU écrans à 0 % (Bilan, Meteo, Repas, Souvenirs) | tests | +couverture, régressions écrans captées | **Bilan 0→100, Souvenirs 16→89, Meteo 0→élevé, Repas 9,5→~80 %** | ✅ |
| 2 | E2E — pattern PCOM (Page/Component Object Model) | tests/outillage | specs maintenables, sélecteurs centralisés | — | ⬜ |
| 3 | E2E — scénarios manquants par écran | tests | couverture E2E fns 40 % → ≥ 70 % | — | ⬜ |
| 4 | Pipeline qualité automatisé (coverage+Trivy+Lighthouse+Sonar) | outillage/ci | audit reproductible à chaque build | **`scripts/quality-audit.sh` + `npm run quality`** (étapes non bloquantes) | ✅ |

## Priorité 2

| # | Action | Domaine | Gain attendu | Gain obtenu | Statut |
|---|---|---|---|---|---|
| 5 | TU modales d'édition (0–25 %) | tests | CRUD sécurisé par les tests | **11 modales couvertes** (les 0 % incluses) | ✅ |
| 6 | TU usePhotos + links/recap/ics/journal | tests | logique métier couverte ≥ 85 % | **links 34→100, recap 55→100, ics 54→93, journal 65→90, usePhotos 15→89 %** | ✅ |
| 7 | E2E — couche BDD Cucumber/Gherkin | tests/outillage | scénarios lisibles métier | — | ⬜ |
| 8 | Dockerfile — USER non-root (Trivy DS-0002) | securite/ci | 0 misconfig HIGH | **différé (risque > bénéfice)** — voir note | ⏸️ |
| 9 | SEO — meta-description + robots.txt | qualite/ux | Lighthouse SEO 82 → ~100 | **SEO 82 → 100** (0 audit en échec), mesuré Lighthouse 11/07 | ✅ |
| 10 | Best-practices — notif différée + erreurs console | qualite/ux | Lighthouse BP 92 → ~100 | **BP 92→100** (favicon → 404 réglé ; permission web non demandée au chargement). Lighthouse : Perf 94 · A11y 100 · BP 100 · SEO 100 | ✅ |
| 11 | A11y — cliquables accessibles clavier (144 findings) | qualite/ux | -128 bugs+smells Sonar | — | ⬜ |
| 12 | Bug S3923 — ExportModal.jsx:7 | qualite | -1 bug MAJOR, code clarifié | **bugs 65 → 64, reliability C → B, S3923 = 0** (re-scan Sonar 11/07) | ✅ |

## Priorité 3

| # | Action | Domaine | Gain attendu | Gain obtenu | Statut |
|---|---|---|---|---|---|
| 13 | TU App.jsx après extraction | tests/refactor | couverture globale ↑ | — | ⬜ |
| 14 | Keystore hors-repo pour Play Store | securite/release | publication possible | — | ⬜ |
| 15 | Perf — JS inutilisé / render-blocking / bf-cache | qualite | LCP < 2 s | **code-splitting : 8 sous-écrans en chunks lazy (~35 KB différés)** ; Lighthouse perf stable 94 (FCP/LCP dominés par fonts, pas le JS) | ✅ |
| 16 | Math.random S2245 ×6 — annoter/crypto.randomUUID | securite | 0 vuln Sonar | **vulns 6→4** (ics+photos → randomUUID ; 4 Confetti restants = animation, non-crypto) | ✅ |
| 17 | Code smells — vars/imports morts, index-key, parseInt, `?.` | qualite/refactor | -66 smells, dette ↓ | **smells 190→164 (−26), dette 937→864 min** ; S1128 6→1, S1481 9→1, S1854 9→1 | ✅ |

## Journal de traitement

- **11/07** — Action #12 (bug S3923) : `ExportModal.jsx:7` `sx = (css) => s(darkMode ? css : css)`
  → `sx = s` (ternaire mort, reliquat d'un TODO dark-mode ; thème appliqué globalement via
  `body.dark` + `color:inherit`). **Gain** : bugs 65→64, reliability **C→B**, S3923=0. 64 TU verts, build OK.
- **11/07** — Action #9 (SEO) : ajout `<meta name="description">` dans `index.html` + `public/robots.txt`.
  **Gain** : Lighthouse **SEO 82→100**, plus aucun audit SEO en échec. Build OK.
- **11/07** — Action #16 (S2245) : `crypto.randomUUID()` pour l'UID .ics et l'id photo.
  **Gain** : **vulns 6→4** (4 restants = `Math.random` d'animation Confetti, non-crypto, à marquer « Safe »). 310 TU verts.
- **11/07** — Action #17 (code smells) : retrait des reliquats d'extraction des modales
  (imports/fonctions/vars morts) dans `App.jsx`, `EditVisitModal`, `EditExpenseModal`.
  **Gain** : **smells 190→164 (−26), dette 937→864 min**. ⚠️ Piège : `setExportCopied`
  est transmis en prop à l'Accueil → retirer la paire `useState` entière cassait 85 TU ;
  paire conservée. 310 TU verts, build OK. _Branche feature/qualite-sonar-cleanup._
- **12/07** — Action #10 (best-practices) : favicon déclaré dans `index.html` (tue le 404
  `/favicon.ico`) + `dispatchWebNotifications` ne demande plus la permission au chargement
  (ajout de `requestWebNotificationPermission()` pour un futur geste explicite ; natif
  Android inchangé). **Gain** : Lighthouse **BP 92→100** (all-100 : Perf 94 · A11y 100 · BP 100 · SEO 100).
  +1 TU (311 verts). Lot batché avec le README (option B) → un seul merge/deploy.
- **12/07 — Lot A (couverture, #1+#6+#5)** : +57 tests (311→**368**), 1 branche `feature/qualite-tu`,
  3 commits, 1 merge. **Couverture globale : stmts 65→75 %, branches 67→75 %, fns 59→72 %,
  lignes 69→80 %.** Détail : écrans Bilan/Meteo/Repas/Souvenirs, modules links/recap/ics/journal,
  hook usePhotos, 11 modales. Lot tests-only → APK 63 identique au 62 (versionCode seul).
- **12/07 — Lot B (#15 perf + #4 pipeline)** : `feature/qualite-lot-b`.
  - **#15** : code-splitting des 8 sous-écrans (React.lazy + Suspense), ~35 KB différés,
    8 chunks séparés au build ; 6 tests UI adaptés en `findBy`. Lighthouse perf stable 94.
  - **#4** : `scripts/quality-audit.sh` (+ `npm run quality`) — coverage/Trivy/Lighthouse/Sonar
    en étapes non bloquantes. Validé sur l'étape Trivy.
- **12/07 — Action #8 (Dockerfile USER) — DIFFÉRÉE.** Raison : le pipeline de build est
  **fragile et critique** (règles CLAUDE.md) et injecte le source par `docker cp` (fichiers
  root-owned) → un `USER` non-root ne pourrait pas réécrire `/workspace` au runtime. Le
  finding DS-0002 porte sur un **conteneur de build local éphémère** (pas un service déployé),
  donc risque réel quasi nul. Décision : ne pas risquer la casse du build familial pour un
  bénéfice marginal. À reprendre dans un chantier dédié (user `builder` + `chown` +
  `GRADLE_USER_HOME` + `COPY --chown` + adaptation de `build-docker.sh`), validé par un build
  APK complet hors pipeline.
